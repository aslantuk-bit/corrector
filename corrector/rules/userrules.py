"""Правила пользователя из правила.yaml (заполняется в задаче 2)."""

from __future__ import annotations

from pathlib import Path

FILE_NAME = "правила.yaml"


def load_user_rules(path: Path):
    return [], []
